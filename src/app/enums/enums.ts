export enum DifficultyLevels {
  Easy = 'easy',
  Medium = 'medium',
  Hard = 'hard',
}
export enum ProgressTypes {
  Progress = 'progress',
  Regress = 'regress',
}

export enum ButtonColors {
  Blue = 'blue',
  Green = 'green',
}
